"use client";
import { Settings } from "lucide-react"; import { useAuthStore } from "@/stores/auth-store";
export default function Page() { const user = useAuthStore((state) => state.user); return <section className="feature-page"><header className="feature-head"><span className="kicker">SETTINGS</span><div className="feature-title"><div className="feature-icon large"><Settings/></div><h1>Your EVA profile.</h1></div><p>Account and experience preferences for this migration preview.</p></header><div className="settings-card"><div><span>Display name</span><strong>{user?.full_name || user?.username}</strong></div><div><span>Email</span><strong>{user?.email}</strong></div><div><span>Profile</span><strong>{user?.profile_type.replaceAll("_", " ")}</strong></div><div><span>Role</span><strong>{user?.role}</strong></div></div></section>; }
